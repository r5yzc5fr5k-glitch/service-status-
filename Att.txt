<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<title>AT&T Account | Service Status</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<style>
  body {
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
    background-color: #eef1f4;
    color: #1a1a1a;
  }

  .wrapper {
    max-width: 420px;
    margin: 0 auto;
    background: #ffffff;
    min-height: 100vh;
  }

  .header {
    background-color: #002a5c;
    color: #ffffff;
    padding: 16px;
  }

  .header h1 {
    margin: 0;
    font-size: 17px;
    font-weight: 600;
  }

  .subheader {
    font-size: 12px;
    opacity: 0.85;
    margin-top: 4px;
  }

  .content {
    padding: 20px 16px;
  }

  .meta {
    font-size: 13px;
    color: #555;
    margin-bottom: 14px;
  }

  .globe-divider {
    height: 1px;
    background: linear-gradient(to right, transparent, #0057b8, transparent);
    margin: 18px 0;
  }

  .status-bar {
    display: flex;
    justify-content: space-between;
    margin-bottom: 6px;
  }

  .status-step {
    flex: 1;
    text-align: center;
    font-size: 11px;
    color: #999;
  }

  .status-step.active {
    color: #0057b8;
    font-weight: 600;
  }

  .status-line {
    height: 4px;
    background: #d6dde6;
    border-radius: 2px;
    position: relative;
    margin-bottom: 18px;
  }

  .status-progress {
    height: 4px;
    background: #0057b8;
    border-radius: 2px;
    width: 66%;
    transition: width 0.6s ease;
  }

  .spinner {
    width: 22px;
    height: 22px;
    border: 3px solid #d6dde6;
    border-top: 3px solid #0057b8;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin: 14px auto;
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }

  .alert {
    background-color: #f0f4f8;
    border-left: 4px solid #0057b8;
    padding: 14px;
    margin-bottom: 18px;
  }

  .alert strong {
    display: block;
    margin-bottom: 6px;
  }

  .text {
    font-size: 15px;
    line-height: 1.6;
  }

  .footer {
    margin-top: 26px;
    font-size: 12px;
    color: #777;
  }
</style>
</head>
<body>

<div class="wrapper">
  <div class="header">
    <h1>AT&T Internet Service Status</h1>
    <div class="subheader">Account & Network Notifications</div>
  </div>

  <div class="content">
    <div class="meta">
      Date: <span id="today"></span><br>
      Ticket #: <strong id="ticket"></strong><br>
      Affected area: ZIP <strong>10001</strong><br>
      Reported at: <span id="reported"></span><br>
      Last updated: <span id="updated"></span>
    </div>

    <div class="globe-divider"></div>

    <div class="status-bar">
      <div class="status-step active" id="step1">Outage</div>
      <div class="status-step active" id="step2">In Progress</div>
      <div class="status-step" id="step3">Restored</div>
    </div>

    <div class="status-line">
      <div class="status-progress" id="progress"></div>
    </div>

    <div class="spinner" id="spinner"></div>

    <div class="alert" id="alertBox" style="display:none;">
      <strong>Service Interruption</strong>
      AT&T Wi-Fi service is currently unavailable in your area.
    </div>

    <div class="text" id="message" style="display:none;">
      Our technicians are actively working to restore service.
      <br><br>
      <strong>Estimated restoration time:</strong> 1:00 PM today.
      <br><br>
      Please check back for updates.
    </div>

    <div class="footer">
      This information is based on current network diagnostics and may change.
    </div>
  </div>
</div>

<script>
  const now = new Date();

  document.getElementById('today').textContent =
    now.toLocaleDateString(undefined, { year: 'numeric', month: 'long', day: 'numeric' });

  document.getElementById('updated').textContent =
    now.toLocaleTimeString(undefined, { hour: 'numeric', minute: '2-digit' });

  const reported = new Date(now.getTime() - 45 * 60000);
  document.getElementById('reported').textContent =
    reported.toLocaleTimeString(undefined, { hour: 'numeric', minute: '2-digit' });

  document.getElementById('ticket').textContent =
    'INC-' + Math.floor(1000000 + Math.random() * 9000000);

  setTimeout(() => {
    document.getElementById('spinner').style.display = 'none';
    document.getElementById('alertBox').style.display = 'block';
    document.getElementById('message').style.display = 'block';
  }, 1200);

  const restoreTime = new Date();
  restoreTime.setHours(13, 0, 0, 0);

  if (now >= restoreTime) {
    document.getElementById('step3').classList.add('active');
    document.getElementById('progress').style.width = '100%';
    document.getElementById('alertBox').innerHTML =
      '<strong>Service Restored</strong> AT&T Wi-Fi service has been restored.';
    document.getElementById('message').innerHTML =
      'Service has been restored. If issues persist, please restart your equipment or contact AT&T support.';
  }

<script>
  // Freeze date to today, time to 9:30 AM
  const now = new Date();
  const frozenTime = new Date();
  frozenTime.setHours(9, 30, 0, 0);

  document.getElementById('today').textContent =
    now.toLocaleDateString(undefined, { year: 'numeric', month: 'long', day: 'numeric' });

  document.getElementById('updated').textContent = '9:30 AM';
  document.getElementById('reported').textContent = '9:30 AM';

  document.getElementById('ticket').textContent =
    'INC-' + Math.floor(1000000 + Math.random() * 9000000);

  // Spinner behavior (still shows briefly)
  setTimeout(() => {
    document.getElementById('spinner').style.display = 'none';
    document.getElementById('alertBox').style.display = 'block';
    document.getElementById('message').style.display = 'block';
  }, 1200);

  // Restoration logic disabled until after 1:00 PM
  // (Page will remain "In Progress")
</script>













